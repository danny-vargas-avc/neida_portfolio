# Nuxt
src/site/.nuxt/
src/site/.output/
src/site/.data/
src/site/dist/

# Dependencies
src/site/node_modules/

# Logs
*.log
logs/

# Environment
.env
.env.*
# Templates carry no secrets and are the only record of what must be set, so a
# fresh clone needs them. The narrower !.env.example missed
# .env.production.example, which .env.* had already excluded.
!.env*.example
etc/docker/.env.production

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
