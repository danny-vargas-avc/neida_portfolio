# npm 11.5.x crashes resolving Nuxt's peer graph with
# "Cannot read properties of null (reading 'edgesOut')". Setting it here rather
# than relying on a flag means CI and Vercel install cleanly too, whatever npm
# version they happen to run.
legacy-peer-deps=true
